#include <unistd.h>

/*
 * write
 * Write a buffer in file descriptor
 *
 * Header: unistd.h
 * Prototype: int	write(int fd, char *str, int buffer);
 *
 * fd -> file descriptor that write will.... write
 * str -> string address that write will use as source
 * buffer -> bytes of buffer that will be writed
 *
 * Return: write return the number of byte that he writed, in case of error write return -1
 */

int	main(int argc, char argv[])
{
	set
}
