#include "minishell.h"

void	test(void)
{
	/*int		bytes = 1;
	int		fd;
	char	buff[43];

	fd = msh_heredoc("ola");
	while (bytes)
	{

		bytes = read(fd, buff, 42);
		*(buff + bytes) = 0;
		printf ("%s", buff);
	}
	exit (0);*/
}

